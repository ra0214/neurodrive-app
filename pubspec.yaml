name: neurodrive
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  device_preview: ^1.3.1
  provider: ^6.1.2
  http: ^1.2.1
  intl: ^0.20.2
  cupertino_icons: ^1.0.8
  flutter_secure_storage: ^9.2.4
  
  # Seguridad moderna y compatible
  screen_protector: ^1.5.1
  safe_device: ^1.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
